import { Check, ExternalLink, X } from "lucide-react";
import Link from "next/link";
import { notFound } from "next/navigation";
import { AppShell } from "@/components/app-shell";
import { ComparisonDialog } from "@/components/comparison-dialog";
import { PriceValue } from "@/components/ui";
import { modelByCanonicalId, providerById } from "@/lib/catalog";
import { compactNumber, formatDate } from "@/lib/format";
import { abilityMsg, msg } from "@/lib/i18n";
import { getLocale } from "@/lib/server-i18n";

export default async function ModelDetailPage({ params }: { params: Promise<{ canonicalId: string[] }> }) {
  const locale = await getLocale(); const { canonicalId } = await params; const id = canonicalId.map(decodeURIComponent).join("/"); const model = modelByCanonicalId.get(id); if (!model) notFound();
  const providerNames = Object.fromEntries(model.channels.map((channel) => [channel.providerId, providerById.get(channel.providerId)?.name ?? channel.providerId]));
  const initials = model.ownerId.slice(0, 2).toUpperCase();
  return <AppShell locale={locale} section={msg(locale, "models")} detail={model.name}><div className="detail-header"><div className="identity"><span className="identity-mark">{initials}</span><div><h1>{model.name}</h1><p>{model.canonicalId}</p></div>{model.openWeights && <span className="tag success">Open weights</span>}</div><div className="detail-actions"><Link className="secondary-button" href="/models">{msg(locale, "back")}</Link><ComparisonDialog locale={locale} canonicalId={model.canonicalId} channels={model.channels} providerNames={providerNames} /></div></div><div className="detail-metrics"><div><span>{msg(locale, "context")}</span><strong>{compactNumber(model.contextWindow)}</strong></div><div><span>Max output</span><strong>{compactNumber(model.maxOutput)}</strong></div><div><span>{msg(locale, "channels")}</span><strong>{model.providerCount}</strong></div><div><span>{msg(locale, "qualityEvidence")}</span><strong>{model.quality?.aaIndex ?? "-"}</strong></div></div><div className="detail-grid"><div className="detail-main"><section className="panel"><header className="panel-header"><h2>{msg(locale, "modelAbilities")}</h2><span className="mono">{model.family ?? model.ownerId}</span></header><div className="panel-body ability-grid">{Object.entries(model.abilities).map(([key, enabled]) => <div className="ability-item" key={key}><span>{abilityMsg(locale, key)}</span>{enabled ? <Check className="yes" size={15} /> : <X className="no" size={15} />}</div>)}</div></section><section className="panel"><header className="panel-header"><h2>{msg(locale, "pricingOverview")}</h2><span className="mono">{msg(locale, "priceUnit")}</span></header><div className="table-frame borderless"><table className="data-table"><thead><tr><th>{msg(locale, "currency")}</th><th>{msg(locale, "inputPrice")}</th><th>{msg(locale, "outputPrice")}</th><th>{msg(locale, "cacheReadPrice")}</th><th>{msg(locale, "cacheCreationPrice")}</th><th>{msg(locale, "source")}</th></tr></thead><tbody>{(["USD", "CNY"] as const).map((currency) => <tr key={currency}><td className="mono">{currency}</td><td><PriceValue price={model.minPrices[currency]} rate="textInput" currency={currency} locale={locale} /></td><td><PriceValue price={model.minPrices[currency]} rate="textOutput" currency={currency} locale={locale} /></td><td><PriceValue price={model.minPrices[currency]} rate="textInput_cacheRead" currency={currency} locale={locale} /></td><td><PriceValue price={model.minPrices[currency]} rate="textInput_cacheWrite" currency={currency} locale={locale} /></td><td><span className="tag">{model.minPrices[currency]?.source ?? "-"}</span></td></tr>)}</tbody></table></div></section><section className="panel"><header className="panel-header"><h2>{msg(locale, "providerPreview")}</h2><span className="mono">{model.channels.length}</span></header><div className="table-frame borderless"><table className="data-table"><thead><tr><th>{msg(locale, "providers")}</th><th>USD</th><th>CNY</th><th>{msg(locale, "source")}</th></tr></thead><tbody>{model.channels.slice(0, 6).map((channel) => <tr key={channel.id}><td><Link className="entity-name" href={`/providers/${encodeURIComponent(channel.providerId)}`}>{providerNames[channel.providerId]}<small>{channel.providerId}</small></Link></td><td><PriceValue price={channel.displayPrices.USD} rate="textInput" currency="USD" locale={locale} /></td><td><PriceValue price={channel.displayPrices.CNY} rate="textInput" currency="CNY" locale={locale} /></td><td><div className="tag-list">{channel.sourceRefs.slice(0, 2).map((ref) => <span className="tag" key={`${ref.source}:${ref.id}`}>{ref.source}</span>)}</div></td></tr>)}</tbody></table></div></section></div><aside className="detail-side"><section className="panel"><header className="panel-header"><h2>{msg(locale, "details")}</h2></header><div className="panel-body"><dl className="definition-list"><dt>Owner</dt><dd>{model.ownerId}</dd><dt>Released</dt><dd>{model.releasedAt ?? "-"}</dd><dt>Knowledge cutoff</dt><dd>{model.knowledgeCutoff ?? "-"}</dd><dt>Input</dt><dd>{model.modalities?.input?.join(", ") ?? "-"}</dd><dt>Output</dt><dd>{model.modalities?.output?.join(", ") ?? "-"}</dd></dl></div></section>{model.quality && <section className="panel panel-muted"><header className="panel-header"><h2>{msg(locale, "qualityEvidence")}</h2></header><div className="panel-body"><dl className="definition-list"><dt>AAIndex</dt><dd>{model.quality.aaIndex}</dd><dt>{msg(locale, "source")}</dt><dd>{model.quality.source}</dd><dt>Source model</dt><dd>{model.quality.sourceModel}</dd><dt>Revision</dt><dd className="mono">{model.quality.revision.slice(0, 8)}</dd><dt>{msg(locale, "observedAt")}</dt><dd>{formatDate(model.quality.observedAt)}</dd></dl></div></section>}<section className="panel"><header className="panel-header"><h2>{msg(locale, "traceability")}</h2></header><div className="panel-body"><div className="source-stack">{model.sourceRefs.map((ref) => <div key={`${ref.source}:${ref.id}`}><span className="tag">{ref.source}</span><code>{ref.id}</code></div>)}</div></div></section></aside></div></AppShell>;
}
